---
title: Spinner
icon: hourglass_empty
description: Indicate the loading state of a component or page with BootCell spinners, built entirely with HTML, CSS, and no JavaScript.
---

import { Spinner } from 'material-cell/source/Spinner';

import { Example } from '../../../source/component/Example';

<Example>
    <Spinner />
</Example>

```TSX
import { render, createCell } from 'web-cell';
import { Spinner } from 'material-cell/source/Spinner';

render(
    <Spinner />
);
```

Add additional colour properties (e.g. brand colours: `primary`, `secondary`,
or helper colours: `danger`, `info`, `success`, `warning`) to change the appearance of individual circular progress.

<Example>
    <Spinner color="primary" />
</Example>

```TSX
import { render, createCell } from 'web-cell';
import { Spinner } from 'material-cell/source/Spinner';

render(
    <Spinner color="primary" />
);
```
